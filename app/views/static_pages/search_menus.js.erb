if ($('#ajax-response').size() === 0) {
  $('form').append('<div id="ajax-response"></div>');
} else {
  $('#ajax-response').html('');
}
$('#ajax-response').html('<%=j render partial: "ajax_container", collections: @cafemenu_url %>')
